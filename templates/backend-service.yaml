apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: app
spec:
  type: LoadBalancer
  selector:
    app: {{ .Values.backendImage.name }}
  ports:
  - port: 80
    targetPort: 80
